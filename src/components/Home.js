const { default: Navbar } = require("./Navbar")

const Home = (props) => {
    const {isLoggedIn, setLoggedIn, currentUser, setCurrentUser} = props
    return (
        <div>
            Hey
        </div>
    )
}

export default Home